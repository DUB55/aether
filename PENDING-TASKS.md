1. Add AI image generation functionality + functionality so the AI generated image can be used in the websites / web apps that are made.
2. Add agent mode: thta builds complete apps (and that can take a lot of time think about an hour, or multiple hours. it should be able to make a fully complete production ready app.
3. Add things like privacy statements, TOS, license, copyright, etc. so this web app is really professional and production ready.
4. Add functionality for toggleable ads + pricing (stripe test/simulation) (it should be toggleable by me by e.g. modifying a hardcoded value in a json file)
5. Add functionality so the AI can browse the web.
6. Add deep research mode that lets the AI browse the web and generate an analysis in a pdf file / md file / artifact.
7. Add functionality for visual edit mode that works by letting the user select an element and give a prompt.
8. Add functionality so user can preview the website/web app/app that was generated for different screen sizes (including mobile).
9. Add reasoning/thinking mode so the AI reasons/thinks before executing.
10. Add plan mode so the AI plans before executing; generating a plan, a detailed planning with phases, taskslist, requirementslist, blueprint, etc.
11. Add functionality so the user can see percentages for progress and estimation on how much time left until the AI is done building the app + functionality so user sees what the AI is doing at the moment (live).